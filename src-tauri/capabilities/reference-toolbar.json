{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "reference-toolbar",
  "description": "Allows only the local web-search toolbar to call its restricted browser controls",
  "webviews": ["reference-toolbar"],
  "permissions": ["core:default"]
}
